watchdog>=3.0.0
pyyaml>=6.0
fastapi>=0.100.0
uvicorn>=0.23.0
jinja2>=3.0.0
pymysql>=1.1.0
# Windows 服务模式需要（service.py）
pywin32>=306
# web/__init__.py 直接 import（fastapi 的传递依赖，显式声明）
starlette>=0.27.0
# fastapi.testclient 需要（test/test_e2e.py、test/test_web.py）
httpx>=0.24.0
